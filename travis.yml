language: python
python:
  - "3.7"
  - "3.8"

# command to install dependencies
install:
  - pip install pandas
  - pip install numpy
  - pip install sklearn
  - pip install pytest
# command to run tests
script: pytest tests/test.py
